/// Represents the minimum width of a pane in a vertical split to prevent collapsing panes completely.
pub const MIN_VERTICAL_SPLIT_PANE_WIDTH: f64 = 200.0;
/// Represents the minimum height of a pane in a horizontal split to prevent collapsing panes completely.
pub const MIN_HORIZONTAL_SPLIT_PANE_HEIGHT: f64 = 50.0;

/// Determines how often the playback position is polled to update the playback bar.
pub const CURRENT_PLAYBACK_POSITION_POLL_INTERVAL_MS: u64 = 100;
